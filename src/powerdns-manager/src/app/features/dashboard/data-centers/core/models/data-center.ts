export interface DataCenter {
    id: string | undefined,
    name: string,
    address?: string,
    city?: string,
    postalCode?: string,
    region?: string,
    country?: string,
    description?: string
}