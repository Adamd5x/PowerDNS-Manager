import { StartUpConfig } from "./startup-config";

export const EMPTY_STARTUP : StartUpConfig = {
    licenseToken: undefined,
    userToken: undefined,
    refreshInterval: 0,
    endpoints: undefined
}